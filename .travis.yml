language: cpp
dist: bionic

compiler:
  -gcc
os: linux

before_install:
  - echo Starting Build Process

script:
  - if [ $TRAVIS_OS_NAME == linux ]; then bash ./configure.sh; fi
  - cd ./Tests; make; ./Build/tests; cd ..

notifications:
  email: false
